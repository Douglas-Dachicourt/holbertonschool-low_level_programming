#include <stdio.h>
/**
 * main - écrit la phrase avec printf
 *
 * Return: 0 si tout se passe bien
 */
int main(void)
{
    printf("with proper grammar, but the outcome is a piece of art,\n");
    return (0);
}